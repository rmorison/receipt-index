services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: receipt_index_dev
      POSTGRES_USER: receipt_index
      POSTGRES_PASSWORD: localpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U receipt_index -d receipt_index_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
